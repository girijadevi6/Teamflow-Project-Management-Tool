# Frontend environment variables
# Copy this file to frontend/.env for local overrides, or set VITE_API_URL
# in Render's dashboard for the deployed build.

# URL of the deployed backend API (no trailing slash)
VITE_API_URL=http://localhost:8000
